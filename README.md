# Angular-nebular stub

# Step1 : load dependencies
npm install

# Step2 : load devDependecies, normally should be @angular-devkit/build-angular missing
npm install @angular-devkit/build-angular